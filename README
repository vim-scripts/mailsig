This is a mirror of http://www.vim.org/scripts/script.php?script_id=509

This plugin provides a :Sign command which provides a list of signatures in your ~/.sigs file, and then inserts the one you select at the bottom of your mail message, automatically word-wrapping the first line of the signature.
